<form method="post" action="{{ path('admin_friendlink_delete', {'id': friend_link.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ friend_link.id) }}">
    <button class="btn">Supprimer</button>
</form>
